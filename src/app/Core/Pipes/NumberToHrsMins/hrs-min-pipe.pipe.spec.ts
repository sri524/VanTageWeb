import { HrsMinPipePipe } from './hrs-min-pipe.pipe';

describe('HrsMinPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HrsMinPipePipe();
    expect(pipe).toBeTruthy();
  });
});
